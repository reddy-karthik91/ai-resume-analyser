import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class ApiService {
  // api.service.ts
  // Centralized HTTP client wrapper service.
  //
  // Future implementation details:
  // Integrate Angular HttpClient to wrap GET, POST, PUT, DELETE requests with standard error catches,
  // mapping, and header definitions.
  constructor() {}
}
